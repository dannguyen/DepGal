<h1>Images</h1>

<% @images.each do |image| %>

	<div class='image'>
		<% i_path = image.thumbnail_path(rand(3))
			dim = FastImage.size("#{RAILS_ROOT}/public#{i_path}")
		%>
		<%=link_to(image_tag(i_path, :width=>dim.andand[0], :height=>dim.andand[1]), image)%>	
	</div>
<% end %>